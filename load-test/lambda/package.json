{
  "name": "playwright-lambda-load-test",
  "version": "1.0.0",
  "description": "Playwright load testing on AWS Lambda",
  "main": "index.js",
  "dependencies": {
    "@playwright/test": "^1.55.0",
    "playwright": "^1.55.0"
  },
  "scripts": {
    "build": "docker build -t playwright-lambda .",
    "test-local": "node index.js"
  }
}